#!/bin/bash

export PATH="$HOME/.local/share/omadora/bin:$PATH"

# Set to true when going directly to a submenu, so we can exit directly
BACK_TO_EXIT=false

back_to() {
  local parent_menu="$1"

  if [[ "$BACK_TO_EXIT" == "true" ]]; then
    exit 0
  elif [[ -n "$parent_menu" ]]; then
    "$parent_menu"
  else
    show_main_menu
  fi
}

menu() {
  local prompt="$1"
  local options="$2"
  local extra="$3"
  local preselect="$4"

  read -r -a args <<<"$extra"

  if [[ -n "$preselect" ]]; then
    local index
    index=$(echo -e "$options" | grep -nxF "$preselect" | cut -d: -f1)
    if [[ -n "$index" ]]; then
      args+=("-a" "$index")
    fi
  fi

  #echo -e "$options" | walker --dmenu --theme dmenu_250 -p "$prompt…" "${args[@]}"
  echo -e "$options" | wofi --dmenu -p "$prompt…" "${args[@]}"
}

terminal() {
  alacritty --class=Omadora -e "$@"
}

present_terminal() {
  omadora-launch-floating-terminal-with-presentation "$1"
}

open_in_editor() {
  notify-send "Editing config file" "$1"
  omadora-launch-editor "$1"
}

install() {
  present_terminal "echo 'Installing $1...'; sudo dnf install -y $2"
}

install_and_launch() {
  present_terminal "echo 'Installing $1...'; sudo dnf install -y $2 && setsid gtk-launch $3"
}

install_font() {
  present_terminal "echo 'Installing $1...'; sudo dnf install -y $2 && sleep 2 && omadora-font-set '$3'"
}

show_learn_menu() {
  case $(menu "Learn" "  Keybindings\n  Omadora\n  Omarchy\n  Hyprland\n  Fedora\n󰣇  Arch\n  Neovim\n󱆃  Bash") in
  *Keybindings*) omadora-menu-keybindings ;;
  *Omadora*) omadora-launch-webapp "https://github.com/elpritchos/omadora" ;;
  *Omarchy*) omadora-launch-webapp "https://learn.omacom.io/2/the-omarchy-manual" ;;
  *Hyprland*) omadora-launch-webapp "https://wiki.hypr.land" ;;
  *Fedora*) omadora-launch-webapp "https://docs.fedoraproject.org" ;;
  *Arch*) omadora-launch-webapp "https://wiki.archlinux.org" ;;
  *Neovim*) omadora-launch-webapp "https://neovim.io/doc/user/quickref.html" ;;
  *Bash*) omadora-launch-webapp "https://devhints.io/bash" ;;
  *) show_main_menu ;;
  esac
}

show_trigger_menu() {
  case $(menu "Trigger" "  Capture\n󰔎  Toggle") in
  *Capture*) show_capture_menu ;;
  *Toggle*) show_toggle_menu ;;
  *) show_main_menu ;;
  esac
}

show_capture_menu() {
  case $(menu "Capture" "  Screenshot\n  Screenrecord\n󰃉  Color") in
  *Screenshot*) show_screenshot_menu ;;
  *Screenrecord*) show_screenrecord_menu ;;
  *Color*) pkill hyprpicker || hyprpicker -a ;;
  *) show_trigger_menu ;;
  esac
}

show_screenshot_menu() {
  case $(menu "Screenshot" "  Region\n  Window\n  Display") in
  *Region*) omadora-cmd-screenshot ;;
  *Window*) omadora-cmd-screenshot window ;;
  *Display*) omadora-cmd-screenshot output ;;
  *) show_capture_menu ;;
  esac
}

show_screenrecord_menu() {
  case $(menu "Screenrecord" "  Region\n  Region + Audio\n  Display\n  Display + Audio") in
  *"Region + Audio"*) omadora-cmd-screenrecord region audio ;;
  *Region*) omadora-cmd-screenrecord ;;
  *"Display + Audio"*) omadora-cmd-screenrecord output audio ;;
  *Display*) omadora-cmd-screenrecord output ;;
  *) show_capture_menu ;;
  esac
}

show_toggle_menu() {
  case $(menu "Toggle" "󱄄  Screensaver\n󰔎  Nightlight\n󱫖  Idle Lock\n󰍜  Top Bar") in
  *Screensaver*) omadora-toggle-screensaver ;;
  *Nightlight*) omadora-toggle-nightlight ;;
  *Idle*) omadora-toggle-idle ;;
  *Bar*) omadora-toggle-waybar ;;
  *) show_trigger_menu ;;
  esac
}

show_style_menu() {
  case $(menu "Style" "󰸌  Theme\n  Font\n  Background\n  Hyprland\n󱄄  Screensaver\n  About") in
  *Theme*) show_theme_menu ;;
  *Font*) show_font_menu ;;
  *Background*) omadora-theme-bg-next ;;
  *Hyprland*) open_in_editor ~/.config/hypr/looknfeel.conf ;;
  *Screensaver*) open_in_editor ~/.config/omadora/branding/screensaver.txt ;;
  *About*) open_in_editor ~/.config/omadora/branding/about.txt ;;
  *) show_main_menu ;;
  esac
}

show_theme_menu() {
  theme=$(menu "Theme" "$(omadora-theme-list)" "" "$(omadora-theme-current)")
  if [[ "$theme" == "CNCLD" || -z "$theme" ]]; then
    back_to show_style_menu
  else
    omadora-theme-set "$theme"
  fi
}

show_font_menu() {
  theme=$(menu "Font" "$(omadora-font-list)" "-w 350" "$(omadora-font-current)")
  if [[ "$theme" == "CNCLD" || -z "$theme" ]]; then
    back_to show_style_menu
  else
    omadora-font-set "$theme"
  fi
}

show_setup_menu() {
  local options="  Audio\n  Wifi\n󰂯  Bluetooth\n󱐋  Power Profile\n󰍹  Monitors"
  [ -f ~/.config/hypr/bindings.conf ] && options="$options\n  Keybindings"
  [ -f ~/.config/hypr/input.conf ] && options="$options\n󰍽  Input"
  options="$options\n  Defaults\n󰱔  DNS\n  Config"

  case $(menu "Setup" "$options") in
  *Audio*) omadora-launch-audio ;;
  *Wifi*)
    rfkill unblock wifi
    omadora-launch-wifi
    ;;
  *Bluetooth*)
    rfkill unblock bluetooth
    omadora-launch-bluetooth 
    ;;
  *Power*) show_setup_power_menu ;;
  *Monitors*) open_in_editor ~/.config/hypr/monitors.conf ;;
  *Keybindings*) open_in_editor ~/.config/hypr/bindings.conf ;;
  *Input*) open_in_editor ~/.config/hypr/input.conf ;;
  *Defaults*) open_in_editor ~/.config/uwsm/default ;;
  *DNS*) present_terminal omadora-setup-dns ;;
  *Config*) show_setup_config_menu ;;
  *) show_main_menu ;;
  esac
}

show_setup_power_menu() {
  profile=$(menu "Power Profile" "$(omadora-powerprofiles-list)" "" "$(powerprofilesctl get)")

  if [[ "$profile" == "CNCLD" || -z "$profile" ]]; then
    back_to show_setup_menu
  else
    omadora-powerprofiles-set "$profile"
  fi
}

show_setup_config_menu() {
  #case $(menu "Setup" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n  Swayosd\n󰌧  Walker\n󰍜  Waybar\n󰞅  XCompose") in
  case $(menu "Setup" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n󰌧  Wofi\n󰍜  Waybar\n󰞅  XCompose") in
  *Hyprland*) open_in_editor ~/.config/hypr/hyprland.conf ;;
  *Hypridle*) open_in_editor ~/.config/hypr/hypridle.conf && omadora-restart-hypridle ;;
  *Hyprlock*) open_in_editor ~/.config/hypr/hyprlock.conf ;;
  *Hyprsunset*) open_in_editor ~/.config/hypr/hyprsunset.conf && omadora-restart-hyprsunset ;;
  #*Swayosd*) open_in_editor ~/.config/swayosd/config.toml && omadora-restart-swayosd ;;
  #*Walker*) open_in_editor ~/.config/walker/config.toml && omadora-restart-walker ;;
  *Wofi*) open_in_editor ~/.config/wofi/config && omadora-restart-wofi ;;
  *Waybar*) open_in_editor ~/.config/waybar/config.jsonc && omadora-restart-waybar ;;
  *XCompose*) open_in_editor ~/.XCompose && omadora-restart-xcompose ;;
  *) show_main_menu ;;
  esac
}

show_install_menu() {
  case $(menu "Install" "  Package\n  Web App\n  TUI\n  Style") in
  *Package*) terminal omadora-pkg-install ;;
  *Web*) present_terminal omadora-webapp-install ;;
  *TUI*) present_terminal omadora-tui-install ;;
  *Style*) show_install_style_menu ;;
  *) show_main_menu ;;
  esac
}

show_install_style_menu() {
  case $(menu "Install" "󰸌  Theme\n  Background") in
  *Theme*) present_terminal omadora-theme-install ;;
  *Background*) nautilus ~/.config/omadora/current/theme/backgrounds ;;
  *) show_install_menu ;;
  esac
}

show_remove_menu() {
  case $(menu "Remove" "  Package\n  Web App\n  TUI\n󰸌  Theme") in
  *Package*) terminal omadora-pkg-remove ;;
  *Web*) present_terminal omadora-webapp-remove ;;
  *TUI*) present_terminal omadora-tui-remove ;;
  *Theme*) present_terminal omadora-theme-remove ;;
  *) show_main_menu ;;
  esac
}

show_update_menu() {
  case $(menu "Update" "  Omadora\n  Config\n󰸌  Themes\n  Process\n󰇅  Hardware\n  Password\n  Timezone") in
  *Omadora*) present_terminal omadora-update ;;
  *Config*) show_update_config_menu ;;
  *Themes*) present_terminal omadora-theme-update ;;
  *Process*) show_update_process_menu ;;
  *Hardware*) show_update_hardware_menu ;;
  *Timezone*) present_terminal omadora-cmd-tzupdate ;;
  *Password*) show_update_password_menu ;;
  *) show_main_menu ;;
  esac
}

show_update_process_menu() {
  #case $(menu "Restart" "  Hypridle\n  Hyprsunset\n  Swayosd\n󰌧  Walker\n󰍜  Waybar") in
  case $(menu "Restart" "  Hypridle\n  Hyprsunset\n󰌧  Wofi\n󰍜  Waybar") in
  *Hypridle*) omadora-restart-hypridle ;;
  *Hyprsunset*) omadora-restart-hyprsunset ;;
  #*Swayosd*) omadora-restart-swayosd ;;
  #*Walker*) omadora-restart-walker ;;
  *Wofi*) omadora-restart-wofi ;;
  *Waybar*) omadora-restart-waybar ;;
  *) show_update_menu ;;
  esac
}

show_update_config_menu() {
  #case $(menu "Use default config" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n󱣴  Plymouth\n  Swayosd\n󰌧  Walker\n󰍜  Waybar") in
  case $(menu "Use default config" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n󱣴  Plymouth\n󰌧  Wofi\n󰍜  Waybar") in
  *Hyprland*) present_terminal omadora-refresh-hyprland ;;
  *Hypridle*) present_terminal omadora-refresh-hypridle ;;
  *Hyprlock*) present_terminal omadora-refresh-hyprlock ;;
  *Hyprsunset*) present_terminal omadora-refresh-hyprsunset ;;
  *Plymouth*) present_terminal omadora-refresh-plymouth ;;
  #*Swayosd*) present_terminal omadora-refresh-swayosd ;;
  #*Walker*) present_terminal omadora-refresh-walker ;;
  *Wofi*) present_terminal omadora-refresh-wofi ;;
  *Waybar*) present_terminal omadora-refresh-waybar ;;
  *) show_update_menu ;;
  esac
}

show_update_hardware_menu() {
  case $(menu "Restart" "  Audio\n󱚾  Wi-Fi\n󰂯  Bluetooth") in
  *Audio*) present_terminal omadora-restart-pipewire ;;
  *Wi-Fi*) present_terminal omadora-restart-wifi ;;
  *Bluetooth*) present_terminal omadora-restart-bluetooth ;;
  *) show_update_menu ;;
  esac
}

show_update_password_menu() {
  case $(menu "Update Password" "  Drive Encryption\n  User") in
  *Drive*) present_terminal omadora-drive-set-password ;;
  *User*) present_terminal passwd ;;
  *) show_update_menu ;;
  esac
}

show_system_menu() {
  case $(menu "System" "  Lock\n󱄄  Screensaver\n󰤄  Suspend\n  Stop\n󰜉  Reboot\n󰐥  Shutdown") in
  *Lock*) omadora-lock-screen ;;
  *Screensaver*) omadora-launch-screensaver force ;;
  *Suspend*) systemctl suspend ;;
  *Stop*) omadora-state clear relaunch-required && uwsm stop ;;
  *Reboot*) omadora-state clear re*-required && systemctl reboot ;;
  *Shutdown*) omadora-state clear re*-required && systemctl poweroff ;;
  *) back_to show_main_menu ;;
  esac
}

show_main_menu() {
  go_to_menu "$(menu "Go" "󰀻  Apps\n󰧑  Learn\n󱓞  Trigger\n  Style\n  Setup\n󰉉  Install\n󰭌  Remove\n  Update\n  About\n  System")"
}

go_to_menu() {
  case "${1,,}" in
  #*apps*) walker -p "Launch…" ;;
  *apps*) wofi --show drun -p "Launch…" ;;
  *learn*) show_learn_menu ;;
  *trigger*) show_trigger_menu ;;
  *style*) show_style_menu ;;
  *theme*) show_theme_menu ;;
  *screenshot*) show_screenshot_menu ;;
  *screenrecord*) show_screenrecord_menu ;;
  *setup*) show_setup_menu ;;
  *power*) show_setup_power_menu ;;
  *install*) show_install_menu ;;
  *remove*) show_remove_menu ;;
  *update*) show_update_menu ;;
  *about*) omadora-launch-about ;;
  *system*) show_system_menu ;;
  esac
}

if [[ -n "$1" ]]; then
  BACK_TO_EXIT=true
  pkill wofi || go_to_menu "$1"
else
  pkill wofi || show_main_menu
fi
